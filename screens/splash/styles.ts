import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  bigText: {
    fontSize: 36,
    textAlign: "center",
  },
  smallText: {
    fontSize: 24,
    textAlign: "center",
  },
  button: {
    //borderWidth: 2,
    backgroundColor: "#D00EDF",
    padding: 24,
    borderRadius: 24,
  },
});
